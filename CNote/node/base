1、基础
    #include <stdio.h>
    #define

    signed 有符号，默认
    unsigned 无符号

    auto 动态存储
    static 静态存储
    register
    extern

2、输入输出函数
    putchar 输出字符数据
    getchar 输入字符数据
    puts 输出字符串
    gets 输入字符串
    printf 输入带格式
    scanf 输入带格式

    d,i 有符号整数
    u 无符号整数
    o 无符号8进制整数
    x,X 无符号16进制整数
    c 用来输入到个字符
    s 用来输入字符串
    f 小数
    e,E 以指数形式
    g,G
    字母l 用于长整型整数
    m代表一个整数 数据最小宽度
    n代表一个整数 对小数表示输出n个小数，对字符串表示截取的字符个数
    _ 输出的数字或字符在域内向左靠

3、
    goto
4、位运算
    &,|,^,~

5、数据类型
    枚举类型

    构造类型:
    数组类型，
    结构体类型，
    共用体类型

    指针类型

6、函数
    main(int argc, char*argv[])
    内部函数 static
    外部函数 extern

7、内存
    malloc
    free
    calloc
    realloc

8、宏
    #define
    #undef
    #include
    #if #endif
    #if #else #endif
    #if #elif #endif
    #ifdef
    #ifndef
    #line 整数[文件名]:
     _LINE_  _FILE_
    #pargam
    _STDC_  _DATE_ _TIME_

9、指针
    库函数
    指针变量
    指向指针的指针的变量 char ** a;
    指针函数:
            数据类型 *fun(args);

    函数指针的声明方法为：
           类型 ( * 指针变量名) ([形参列表]);

10、文件
    struct FILE
    fopen(文件，使用文件的方式)
    gclose(文件指针)
    fputc
    fgetc
    fputs
    fgets
    fprintfb
    fscanf
    fraed
    fwirte
    fseek
    rewind
    ftell
